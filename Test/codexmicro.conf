# Codex Micro harness configuration
#
# The backend names where agent status comes from. "mock" uses the built-in
# simulated agent so the whole system runs with no network or model. This is
# the LLM-agnostic seam: pointing backend at a real adapter later swaps the
# source of status without changing the driver or the harness's control logic.
backend=mock
